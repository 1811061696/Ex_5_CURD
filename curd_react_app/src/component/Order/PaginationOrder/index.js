import classNames from "classnames/bind";
import styles from "./PaginationOrder.module.scss"

const cx = classNames.bind(styles)

function PaginationOrder() {
    return ( <h1>Pagination Order</h1> );
}

export default PaginationOrder;